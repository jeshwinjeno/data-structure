#include <stdio.h>

int main() {
 
    int arr[] = {10, 20, 30, 40, 50, 60, 70, 80, 90};

    
    int index = 5; // 5th index in 1-based indexing

 
    printf("The 5th iterated element (6th element in array) is: %d\n", arr[index]);

    return 0;
}
